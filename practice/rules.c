#include<stdio.h>
int main(void)
{
	int top, score; //什么东西
	top = (int)1.3;
	score = (int)1.6;
//	while (top++ < 20)
//		score = score + top;
	printf("top = %d, score = %d\n",top, score);
	return 0;
}
